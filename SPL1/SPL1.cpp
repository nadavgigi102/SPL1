#include "Studio.h"
#include <iostream>

using namespace std;

Studio* backup = nullptr;

int main() {
	std::string s = "fsfs";
	//Workout work = Workout(0, s, 50, MIXED);
	//cout << work.getName()  ;
	return 0;
}